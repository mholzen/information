package rel

// type Verb PropertyPair[Property] // what happens to a property

// var timeProperty = NewProperty()
// var position = NewProperty()
// var different = NewProperty()
// var same = NewProperty()

// var change = Verb{left: timeProperty, right: different}
// var remain = Verb{left: timeProperty, right: same}

// var alive = NewProperty()

// var die = Verb{left: change, right: alive}  // change over `time` of the property `alive`
// var live = Verb{left: remain, right: alive} // remain over `time` of the property `alive`

// var move = Verb{left: change, right: position}

// var inside = NewProperty()
// var consume = Verb{left: change, right: inside}
